// Mirrors backend/app/happy_code.py — kept in sync manually since this is a small POC.
export const NUM_QUBITS = 6;
export const S_MAX = 1.0; // bits, max single-qubit entropy (boundary node color scale)
export const S_EDGE_MAX = 2.0; // bits, max two-qubit block entropy (edge thickness/color scale)
export const R_MAX = 200.0; // px, boundary circle radius
export const R_MIN = 40.0; // px, min bulk node radius
